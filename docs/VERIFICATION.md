# 验证与限制

验证日期：2026-09-19。以下记录实际执行结果，未完成项不视为通过。

## 已执行

- Go 测试及 race 检查、go vet：通过。覆盖尺寸/金额解析、未知尺寸、幂等与版本冲突、余料筛选、回收站、照片唯一归属、上传重试、带照片的备份恢复、缺图失败、维护锁、缺库拒绝与 CSV 注入。
- Vue 类型检查、Vite 生产构建、前端单元测试：通过。
- 独立 Chrome 自动化：375×667 下 HEIC 上传 → 保存材质尺寸 → 更新余料 → 历史 → 删除 → 恢复，通过。320/375/390/768/1440 宽度的录入页无横向溢出，拍照 input 的 capture 与相册 multiple 属性检查通过。
- 本地 macOS libvips 8.18.6 与目标 Ubuntu libvips 8.18.0 均成功读取 libheif 项目的 example.heic，输出可显示的 sRGB JPEG，输出头无 EXIF/GPS。样本只用于忽略目录中的测试，不提交照片到仓库。
- 发布和安装 shell 语法检查：通过。

## 实际限制与未完成验证

- 没有接入真实 iPhone/Android 的系统相机测试。桌面移动尺寸模拟不能证明系统相机选择、取消返回和真实 iPhone 拍摄 HEIC 的全部行为。上线后需用实际手机完成拍照/相册选择确认；当前实现使用系统文件选择器及后置相机提示。
- 当前入口 HTTP，没有 getUserMedia 网页预览、Service Worker、完整 PWA 安装或离线同步。
- 标准照片最长边 2560 px，不保存原始摄影文件。颜色依赖照明和设备，不承诺等同实物。
- 10,000 条记录 p95 与 Fast 4G LCP 尚未测量；方案性能数字仍为目标，不是保证。
- 备份为同盘硬链接快照，没有异机备份；发布前备份和发行历史需人工管理空间。
- 修改历史接口展示最近 100 次；数据库保留记录至布料过期删除。列表返回时筛选保存在 URL，超出第一页的滚动恢复仍可进一步优化。

目标主机隔离服务：48 MP、约 19 MiB JPEG 上传+处理 2.523 秒，输出 2560×1920，主图与缩略图共约 1.52 MiB；服务峰值内存 47.5 MiB，NRestarts=0。HEIC 上传成功，数据库与图片硬链接快照的隔离恢复通过。测试数据在 /opt 下独立目录；服务器 /tmp 为约 868 MiB tmpfs，不能用作具有 5 GiB 空间保护的测试数据盘。首次生产备份、CI/CD 和正式健康检查将在部署验证后补充。
